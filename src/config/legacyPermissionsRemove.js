/**
 * Legacy / retired permission names removed from the product and purged on sync.
 * Keep in sync with userRoutes and any filters.
 */
const LEGACY_PERMISSION_NAMES = [
  "ADMIN_PERMISSIONS",
  "MANAGE_MINING",
  "MANAGE_FOREST",
  "MANAGE_POLLUTION",
  "MANAGE_PAYMENTS",
];

module.exports = { LEGACY_PERMISSION_NAMES };
